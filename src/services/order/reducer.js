const initialStore = {
  number: null,
  name: null,
  pendingRequest: false,
  error: null,
}

export const reducer = (store = initialStore, action) => {
  return store
  // switch (action.type) {
  // case '':
  //   return {
  //     ...store,
  //   }

  // default:
  //   return store
  // }
}
